defmodule GroupManager.ChatterTest do
  use ExUnit.Case

  test "the truth" do
    assert 1 + 1 == 2
  end

  # locate
  # broadcast(gossip)
  # broadcast([nodes], message)
  # get_local_ip
  # local_netid
  # multicast_netid
end
